# ZK Werewolf - Environment Variables Example
# ==============================================
# Copy this file to .env and modify values as needed

# Node.js Environment
NODE_ENV=development

# Logging Configuration
RUST_LOG=debug

# Frontend Configuration (Next.js)
# These are used by the browser to connect to the backend
NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_WS_URL=ws://localhost:8080

# Backend Server URL
# Used by MPC nodes to communicate with the backend server
# Development: localhost, Production: Docker service name (e.g., http://backend:8080)
SERVER_URL=http://localhost:8080

# MPC Node TCP Addresses (Node-to-Node communication for MPC protocol)
# Development: localhost with different ports
ZK_MPC_NODE_0_TCP=localhost:8000
ZK_MPC_NODE_1_TCP=localhost:8001
ZK_MPC_NODE_2_TCP=localhost:8002
# Production: Docker service names
# ZK_MPC_NODE_0_TCP=zk-mpc-node-0:8000
# ZK_MPC_NODE_1_TCP=zk-mpc-node-1:8000
# ZK_MPC_NODE_2_TCP=zk-mpc-node-2:8000

# MPC HTTP Port
MPC_HTTP_PORT=9000

# MPC Node HTTP API URLs (Backend -> MPC Node proof requests)
# Development: localhost with different ports
ZK_MPC_NODE_0_HTTP=http://localhost:9000
ZK_MPC_NODE_1_HTTP=http://localhost:9001
ZK_MPC_NODE_2_HTTP=http://localhost:9002
# Production: Docker service names
# ZK_MPC_NODE_0_HTTP=http://zk-mpc-node-0:9000
# ZK_MPC_NODE_1_HTTP=http://zk-mpc-node-1:9000
# ZK_MPC_NODE_2_HTTP=http://zk-mpc-node-2:9000